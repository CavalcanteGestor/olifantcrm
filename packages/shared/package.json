{
  "name": "@crmolifant/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --target node20 --sourcemap --minify --dts --out-dir dist",
    "lint": "echo \"(shared) lint ainda não configurado\""
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}


